<UserControl x:Class="TestingApp.Views.Pages.TestResult"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestingApp.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="White">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Ваш результат: "/>
                <TextBlock Text="{Binding CurrentFinalResult}"/>
            </StackPanel>
            <Button Width="150" Height="30" Content="В меню" Command="{Binding NavigateMainMenuPageCommand}" Margin="0 15 0 0"/>
        </StackPanel>
        
    </Grid>
</UserControl>
